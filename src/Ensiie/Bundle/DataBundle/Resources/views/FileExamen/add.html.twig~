{% extends 'EnsiieMainBundle:Prof:index.html.twig' %}

{% block body %}

{% if error != '' %}
  <div class="alert alert-error">
	<p data-dismiss="alert">ERROR : {{ error }}</p>
  </div>
{% endif %}
{% if success != '' %}
  <div class="alert alert-success">
	<p data-dismiss="alert">Le fichier {{ success }} a été ajouté avec succès.</p>
  </div>
{% endif %}

<form action="#" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary" value="Déposer" />
</form>


<table class="table table-hover table-bordered" style="width:65%;background-color:white;">    
  <thead style="background-color:#49afcd">
    <tr>
      <th style="">Nom</th>
      <th style="">Fichier</th>
      <th style="">Déposé par</th>
      <th >Télécharger</th>
    </tr>
  </thead>
  <tbody> 
  {% for exam in mes_examens_promo %}	
  <tr>
    <td><p>{{ exam.name }}</p></td>
    <td><p>{{ exam.path }}</p></td>
    <td><p>{{ exam.user.username }}</p></td>
    <td><p><a href="{{ exam.getDlPath() }}" >dl</a></p></td>
  </tr>
  {% endfor %}
  </tbody>
</table>

{% endblock %}