{% extends 'EnsiieMainBundle:Prof:index.html.twig' %}

{% block body %}
<h1>Retirer des Etudiants</h1>
<a class="btn btn-warning" href="{{ path('ensiie_examen_ajouter_etu',{"id":id}) }}">Ajouter des étudiants</a>
<a class="btn btn-warning" href="{{ path('ensiie_examen_retirer_etu',{"id":id}) }}">Retirer des étudiants</a>
{% if error != '' %}
  <div class="alert alert-error">
	<p data-dismiss="alert">ERROR : {{ error }}</p>
  </div>
{% endif %}
{% if success != '' %}
  <div class="alert alert-success">
	<p data-dismiss="alert">{{ success }}</p>
  </div>
{% endif %}
<form action="" method="post" {{ form_enctype(form) }}>
    {{ form_rest(form)}}
    <input type="submit" value="Supprimer" class="btn btn-danger" />
</form>
{% endblock %}